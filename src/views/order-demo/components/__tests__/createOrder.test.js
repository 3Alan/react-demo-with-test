// import React from 'react';
// import CreateOrder from '../CreateOrder';
// import configureMockStore from 'redux-mock-store';
// import thunk from 'redux-thunk';
// import { mount } from 'enzyme';
// import { findTestWrapper } from '../../../../utils/testUtils';
// import { Provider } from 'react-redux';
// import store from '../../../../store';

// describe('test createOrder component', () => {
//   let wrapper;

//   beforeEach(() => {
//     wrapper = mount(<Provider store={store}><CreateOrder /></Provider>);
//   });

//   it('get the correct value', () => {
//     const userElement = findTestWrapper(wrapper, 'user');
//     const numElement = findTestWrapper(wrapper, 'num');
//     const priceElement = findTestWrapper(wrapper, 'price');
//     const submitElement = findTestWrapper(wrapper, 'submit');
//   })
// });
